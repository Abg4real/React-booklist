import React from 'react';

const Navbar = () => {
    return (
        <div className="heading">
            <h2>This is the book list.</h2>
            <small>currently without database</small>
        </div>
    );
}

export default Navbar;